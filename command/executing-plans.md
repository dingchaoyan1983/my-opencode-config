---
description: Execute a written implementation plan in this session with review checkpoints.
agent: build
---

Use the `executing-plans` skill to handle this request.

Pass the user's input to the skill unchanged:

$ARGUMENTS

Follow the skill's workflow and hard rules exactly: load and critically review the plan, raise any questions or concerns with the user before starting, create todos, and execute every task with its specified verifications. Stop immediately and ask for help when blocked, and never start implementation without explicit user consent.